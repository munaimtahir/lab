# API Configuration
# ==============================================================================
# Choose ONE of the following configurations based on your deployment scenario:
# ==============================================================================

# OPTION 1: Production with Nginx Proxy (RECOMMENDED for VPS)
# ------------------------------------------------------------
# When frontend is served by nginx and nginx proxies /api/ to backend
# This is the default for production builds via Docker
VITE_API_URL=/api

# OPTION 2: Direct Backend Access (Development or Standalone Frontend)
# ---------------------------------------------------------------------
# Use this when running frontend separately (pnpm dev) and need to connect
# to a backend running elsewhere

# For VPS backend:
# VITE_API_URL=http://172.235.33.181:8000

# For local backend:
# VITE_API_URL=http://localhost:8000

# OPTION 3: No Variable Set (Uses Code Default)
# ----------------------------------------------
# If VITE_API_URL is not set, the code defaults to http://172.235.33.181:8000
# This ensures the app works on VPS without additional configuration
