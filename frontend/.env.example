# API Configuration
# ==============================================================================
# IMPORTANT: VITE_API_URL must be set for the application to work properly
# ==============================================================================

OPTION 1: Production with Nginx Proxy (RECOMMENDED for VPS)
# ------------------------------------------------------------
# When frontend is served by nginx and nginx proxies /api/ to backend
# This is the default for production builds via Docker
VITE_API_URL=/api

# OPTION 2: Direct Backend Access (Development or Standalone Frontend)
# ---------------------------------------------------------------------
# Use this when running frontend separately (npm dev) and need to connect
# to a backend running elsewhere

# For VPS backend (when not using nginx proxy)
VITE_API_URL=http://172.235.33.181:8000

# For local backend (recommended for local development):
# VITE_API_URL=http://localhost:8000

# ==============================================================================
# Note: If VITE_API_URL is not set, the application will show a console error
# and may not function correctly. Always set this variable in your .env file.
# ==============================================================================
