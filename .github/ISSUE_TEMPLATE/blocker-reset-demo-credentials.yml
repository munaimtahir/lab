
name: [Blocker] Reset All Default Demo Credentials
description: Demo users must not exist in production
title: "[Blocker]: Remove/reset demo passwords before deployment"
labels: ["blocker", "security", "auth"]
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        🔐 Credential Reset Task

        All seed/demo users must be removed or reset before real use.

  - type: checkboxes
    id: task
    attributes:
      label: 🔧 Tasks
      options:
        - label: Remove or disable seed users: `admin123`, `reception123`, etc.
        - label: Regenerate secure passwords and update secrets
        - label: Disable seed_data in production `.env`

  - type: checkboxes
    id: checks
    attributes:
      label: ✅ Final Checks
      options:
        - label: No default users exist in production
        - label: Passwords stored securely
        - label: Login audit verified
